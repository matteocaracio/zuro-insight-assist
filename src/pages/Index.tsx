
import React from 'react';
import Layout from '@/components/Layout';
import AIRecommendation from '@/components/AIDignostic';

const Index = () => {
  return (
    <Layout>
      <AIRecommendation />
    </Layout>
  );
};

export default Index;
